name: cicd

on:
  push:
    branches:
      - main


jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.1
      - name: Install Dependancies
        run: npm i
      - name: Lint
        run: npm run lint
